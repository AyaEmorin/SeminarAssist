/**
 * Autoplay stub — reserved for future autoplay/recommendations.
 * Currently returns null (no auto-queued track).
 */
export async function getAutoplayTrack(
  _guildId: string,
  _currentUri: string,
): Promise<null> {
  return null;
}
