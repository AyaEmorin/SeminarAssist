FROM ghcr.io/lavalink-devs/lavalink:4

# Copy our custom configuration
COPY application.yml /opt/Lavalink/application.yml

# Plugin downloads happen on startup, so we just need the config
EXPOSE 2333
